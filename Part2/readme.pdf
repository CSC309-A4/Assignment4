# Assignment4
Food Share Application

How To Use:
- npm install (this should install the required dependencies, express, express-validator, mongodb, mongoose, body-parser)
- mongod (start the database)
- node server.js (start the server)
- open browser, localhost:3000

Notes:
- The database name is "foodshare", so if you want to do quick queries in the mongo shell type, mongo foodshare



